# Removed tracked SQLite runtime artifacts

# These files were removed from the repository index to prevent accidental
# commits of runtime artifacts which can corrupt DB state and bloat the repo.

Removed files:
- trade_post.db-shm
- trade_post.db-wal
